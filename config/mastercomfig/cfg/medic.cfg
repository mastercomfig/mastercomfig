// The Medigun is lag compensated, and in some situations, Medic does
// need to melee, but the Crossbow is very popular at the moment and it is a
// projectile weapon. Because medigun and melee do not need to target any
// particular body part, this takes precedence.
net_projectiles

exec comfig/game_overrides.cfg // Override values set every game

class_config_medic
