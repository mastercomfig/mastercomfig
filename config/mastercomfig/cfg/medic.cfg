// The Medi Gun is lag compensated, and in some situations, Medic does
// need to melee, but the Crossbow is very popular at the moment and it is a
// projectile weapon. Because the Medi Gun and melee do not need to target any
// particular body part, this takes precedence.
net_projectiles;snapshot_buffer

run_game_overrides // Override values set every game

class_config_medic

exec app/medic.cfg
