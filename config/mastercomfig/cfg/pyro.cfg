// The Flamethrower is lag compensated, but it does not really need the most
// precise hitboxes, and Pyro has flares (projectiles) where fast hitboxes help
// Pyro also is not a very melee centric class.
net_projectiles

run_game_overrides // Override values set every game

class_config_pyro
