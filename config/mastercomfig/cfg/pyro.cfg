// The Flamethrower is lag compensated, and it does not really need the most
// precise hitboxes, but Pyro has flares and reflects where fast hitboxes help.
// Pyro also is not a very melee centric class.
net_projectiles;snapshot_buffer

run_game_overrides // Override values set every game

class_config_pyro

exec app/pyro.cfg
