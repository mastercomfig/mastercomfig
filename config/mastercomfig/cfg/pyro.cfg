// The Flamethrower is lag compensated, but it doesn't really need the most
// precise hitboxes, and Pyro has flares (projectiles) where fast hitboxes help
// Pyro also isn't a very melee centric class.
net_projectiles

exec comfig/game_overrides.cfg // Override values set every game

class_config_pyro
