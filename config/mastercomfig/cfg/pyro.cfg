// Flamethrower is lag compensated, but they don't really need the most
// precise hitboxes, and Pyro has flares (projectiles) where fast hitboxes help
// Pyro also isn't a very melee centric class.
hitboxes_projectiles

exec comfig/game_overrides // Override values set every game

class_config_pyro
