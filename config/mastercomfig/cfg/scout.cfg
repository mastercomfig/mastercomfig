// Scout is very much a hitscan centered class. Having the extra precision
// can ensure every bullet hits as intended. Scout does have some projectiles,
// but the hitscan is much more important and prevalent.
net_hitscan;snapshot_buffer

run_game_overrides // Override values set every game

class_config_scout

exec app/scout.cfg
