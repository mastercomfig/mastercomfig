// Sniper is very hitscan heavy, unless playing Huntsman. Although it is
// unfortunate that we cannot select the ideal networking for this case,
// the more important thing is that we get tick-accurate hitboxes, reliably.
net_hitscan

exec comfig/game_overrides.cfg // Override values set every game

class_config_sniper
