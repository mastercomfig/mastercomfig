// Runs after user configs and addons
mat_savechanges // Make sure we init next time with our material system cvars to prevent reloads
block_antialias // Block mat_antialias
dynamic_background // Run dynamic_background after everything
