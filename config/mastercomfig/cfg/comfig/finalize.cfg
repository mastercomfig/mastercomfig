// Runs after user configs and addons
logo
mat_savechanges;host_writeconfig // Make sure we init next time with our material system cvars to prevent reloads
// HUDs use this command together with the valve.rc file to force reloads. Do not let them.
alias mat_antialias"echo This command is blocked to prevent a material system reload. If you want to change this, use the MSAA Anti-aliasing module."
