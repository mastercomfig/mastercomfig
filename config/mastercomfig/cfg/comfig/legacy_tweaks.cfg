// Currently, this file is used only to execute the legacy OpenGL optimizations
// OpenGL optimizations sets lighting_ex=high
// This makes sure we override presets' definitions, but
// we still respect user custom module preferences
//
// If the user has not added "apply_opengl_opts" to their
// cfg/overrides/setup_hook.cfg
// file, this file does nothing
autoexec_run_opengl_opts
