// Spy is a hitscan and melee centric class, but the parameters for a backstab require more sensitive timing.
// It is not still clear what exactly Spy needs to be successful, but an in-between interp seems to be a 
// good choice for now.
net_spy

exec comfig/game_overrides.cfg // Override values set every game

class_config_spy
