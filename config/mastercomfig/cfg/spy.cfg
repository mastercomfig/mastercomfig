// Spy is a hitscan and melee centric class, but the parameters for a backstab require more sensitive timing.
// It is not still unclear what exactly Spy needs to be successful, but a safe interp with lower prediction settings seems to be a good choice for now.
net_spy

exec comfig/game_overrides // Override values set every game

class_config_spy
